const state = {
    menuItems:[]
}

const getters = {
    getMenuItems: state => state.menuItems
}

//firebaseのデータが変わったらstateも変える
const mutations = {

}

const actions = {
    
}

export default {
    state,
    mutations,
    getters,
    actions
}